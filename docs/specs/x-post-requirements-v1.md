# X 投稿アプリ 要件定義書

> **状態: 実装済み(v1.0)。** 実装は
> [public/production-app/x-post/](../../public/production-app/x-post/)、
> テストは `tests/unit/x-post.mjs`(`node tests/run.mjs x-post`)。

## 1. 文書情報

| 項目 | 内容 |
| --- | --- |
| システム名 | X 投稿アプリ(アプリID `x-post`) |
| バージョン | v1.0 |
| 作成日 | 2026年8月12日 |
| 基底仕様 | [threads-mvp-requirements-v1.md](./threads-mvp-requirements-v1.md) v3.0 |

**本書は差分仕様。** 認証・掲載、端末内保存(下書き・履歴)、Gemini 生成の
方式(KeyStore・エラー分類・フォールバック)、画面構成、セキュリティ方針は
すべて基底仕様(Threads 版)と同一であり、再掲しない。実装も Threads 版の
複製を基にしている(共有モジュール化はしない方針
[repository-structure.md](../repository-structure.md) §4-1)。

## 2. Threads 版との差分

### 2.1 投稿先と intent リンク

- 向き先は `https://x.com/intent/post?text=…`。
- 未ログイン時は X が `redirect_after_login=<intent URL>` でログインへ誘導し、
  本文(改行・ハッシュタグ込み)が保持されたままログイン後に作成画面が開く
  ことを実機確認済み(2026-08-12)。
- 最後の「ポスト」は利用者が押す。X の API・トークンは使わない。

### 2.2 文字数の数え方(280ウェイト)

- X の上限は文字数ではなく **280「ウェイト」**。twitter-text の既定に合わせ、
  次の範囲のコードポイントを 1、それ以外(日本語・絵文字を含む)を 2 と数える。
  `U+0000–U+10FF / U+2000–U+200D / U+2010–U+201F / U+2032–U+2037`
- 日本語だけの本文なら実質約140字。画面のカウントは「n / 280 ウェイト」表示。
- Gemini への指示も「日本語なら140文字以内(全角=2の説明つき)」とする。

### 2.3 その他

- 保存キーは `tsam-x-post-v1`(Threads 版と別。下書き・履歴は混ざらない)。
- Portal 掲載は `id: 'x-post'`、名前「X 投稿」、アイコン「X」。

## 3. 採用しなかった提案とその理由

| 提案 | 不採用の理由 |
| --- | --- |
| twitter-text ライブラリの導入 | 外部ライブラリを増やさない方針。必要なのは重み判定のレンジ4つだけで、自前実装で足りる(URL の23ウェイト特例は、URL 入り投稿が要件化された時点で検討) |
| Threads 版との共有モジュール化 | 基底仕様 §9 と同じ判断(複製で運ぶ) |

## 4. 変更履歴

| 日付 | 版 | 内容 |
| --- | --- | --- |
| 2026-08-12 | v1.0 | Threads 版 v3.0 を基に作成。280ウェイト計数と x.com intent を実装 |
