/*
 * T4: note フォールバック体験
 *
 * 目的（検証計画 §3）: 公式API不在を前提に、「アプリ内下書き→note公開」の摩擦を最小化する。
 *
 * **非公式APIは検証しない。** 検証計画 §2 で「Cookie認証依存・規約/仕様変更リスクがあり
 * 製品には不採用」と先に決着している。ここで再検討しないのは、
 * 一度却下した案を理由を知らずに再提案しないためである（docs/specs/README.md の運用）。
 *
 * ------------------------------------------------------------------
 * 【限定解除・2026-08-08】第1段（発注者個人）に限り Draft Bridge を認める
 * ------------------------------------------------------------------
 * 検証計画 §2 に限定解除を追記した。**このトラック（第2段＝会員向け）の判断は
 * 変わらない。** 会員向けでは整形コピー＋遷移が正式仕様のままである。
 *
 * 第1段でも一想自体は Draft Bridge を実装せず、Note Draft Helper へ
 * バトンを渡す（docs/pipeline/roadmap.md §2）。第1段の実運用で得た
 * 「壊れた回数・復旧時間・コピー方式との実際の差」は
 * docs/pipeline/learnings.md §4 に記録し、**第2段の判断材料にする。**
 *
 * したがって T4 は自動化できる項目がほとんど無い。UX の目視検証が本体になる。
 */

export const meta = {
  id: 'T4',
  title: 'note フォールバック体験',
  goal: '公式API不在を前提に、アプリ内下書き→note公開の摩擦を最小化する',
};

export const items = [
  {
    id: 'T4-1',
    kind: 'manual',
    title: '見出し・装飾を保った本文コピー（Markdown / リッチテキスト）の検証',
    pass: 'note エディタへ貼り付けた際の再現度が実用水準',
    owner:
      '実際に貼って見る以外に確かめようがない。'
      + ' クリップボードへ何を載せるか（text/plain か text/html か両方か）で'
      + ' 再現度が変わるため、両方試して結果を残す',
  },
  {
    id: 'T4-2',
    kind: 'manual',
    title: 'note 新規下書き画面への遷移URL・モバイル挙動',
    pass: 'ワンタップで貼り付け直前まで到達できる',
    owner:
      'デスクトップとスマートフォンの両方で確認する。'
      + ' アプリが入っている端末では遷移先が変わることがある',
  },
  {
    id: 'T4-3',
    kind: 'manual',
    title: '公開URLの手動登録フロー',
    pass: '公開結果として履歴管理（FR-060系）が成立する',
    owner:
      '**guide §2-2 により、履歴は利用者ローカル（IndexedDB）に持つ。**'
      + ' 元の計画にあった PublishedPost テーブルは作らない。'
      + ' 「サーバーに記録が無くても運用が成立するか」がここでの判断点',
  },
];
