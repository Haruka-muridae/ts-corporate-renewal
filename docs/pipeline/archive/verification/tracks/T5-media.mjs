/*
 * T5: TTS・動画レンダリング
 *
 * 目的（検証計画 §3）: テンプレート型動画の品質・時間・コストの実数を得る。
 *
 * guide §4 での追加: T5-6「ffmpeg.wasm 対 サーバーレンダリング（即時削除）の比較」。
 *
 * ここは §2 の非保持方針と最も強く衝突する領域である。動画のレンダリングには
 * まとまった計算資源が要り、素直に作るとサーバーが音声も動画も持つことになる。
 * guide §3 は「別サービスのワーカー → 返却/直接アップロード → 即時削除」を既定とし、
 * ffmpeg.wasm によるブラウザ内レンダリングを比較対象に挙げている。
 *
 * ブラウザ内で完結すれば保持がゼロになるが、端末性能に強く依存する。
 * **どちらを採るかは実測でしか決められない。** それが T5-6 の役割。
 */

export const meta = {
  id: 'T5',
  title: 'TTS・動画レンダリング',
  goal: '品質・時間・コストの実数を得て、レンダリング方式を決める',

  /*
   * v0.5 で凍結。動画化（TTS・レンダリング）が MVP 対象外になったため（guide §8）。
   * **削除しない。** T5-6 の比較観点は再開時にそのまま使う資産である。
   * 凍結の全体像は verification/pipeline/FROZEN.md。
   */
  frozen: true,
  frozenReason: 'v0.5 で動画化が MVP 対象外（guide §8）。拡張フェーズで再開する',
};

export const items = [
  {
    id: 'T5-1',
    kind: 'manual',
    title: '日本語TTS 2〜3サービスの比較（自然さ・読み誤り・SSML/読み調整・単価）',
    pass: '主採用＋予備を決定',
    owner:
      '**guide §6-4 の判断待ち事項。** 比較結果を発注者へ提示して決定を仰ぐ。'
      + ' 同じ原稿を各サービスへ通し、results/T5.md に音声ファイルではなく'
      + ' 「読み誤りの箇所」と「単価」を表で残す（音声はコミットしない）',
    blockedBy: 'guide §6-4',
  },
  {
    id: 'T5-2',
    kind: 'manual',
    title: 'シーンJSON→FFmpeg合成（背景＋テロップ＋音声）のPoC',
    pass: '5分動画をエラーなくレンダリング',
    owner: 'Phase 3 着手条件（検証計画 §5）。ローカルの FFmpeg で先に成立させる',
  },
  {
    id: 'T5-3',
    kind: 'manual',
    title: '字幕とナレーションの同期方式（TTSのタイムスタンプ or 強制アライメント）',
    pass: 'ずれが視聴上気にならない水準',
    owner: 'T5-1 の選定結果に依存（タイムスタンプを返すサービスかどうかで方式が変わる）',
  },
  {
    id: 'T5-4',
    kind: 'manual',
    title: 'レンダリング時間・マシンスペック・1本あたりコスト実測',
    pass: '非同期ジョブのタイムアウト設計とコスト見積りに反映',
    owner: 'T5-6 の比較材料になる',
  },
  {
    id: 'T5-5',
    kind: 'manual',
    title: 'BGM・画像素材の権利条件確認',
    pass: '保持すべき権利情報の項目を確定',
    owner:
      '**guide §2 により MediaAsset テーブルは作らない。**'
      + ' 権利情報は素材そのものに付随する固定の情報なので、'
      + ' 利用者データではなくアプリ同梱の素材メタデータとして持つ。'
      + ' YouTube 監査の説明（review-submissions.md §4-2）で'
      + ' 「ライセンス情報を素材とともに記録している」と書いた根拠にもなる',
  },
  {
    id: 'T5-6',
    kind: 'manual',
    title: '【guide §4 追加】ffmpeg.wasm 対 サーバーレンダリング（即時削除）の比較',
    pass: 'どちらを採るか、判断根拠つきで決まる',
    owner: `以下を同一の題材（5分・シーン10枚程度）で測って表にする。

| 観点 | ffmpeg.wasm（ブラウザ内） | 外部ワーカー（Cloud Run Jobs 等） |
| --- | --- | --- |
| 保持 | **ゼロ**。音声も動画も端末から出ない | 処理中のみ。完了時に即時削除 |
| 所要時間 | 端末性能に依存。実機で測る | サーバー性能で一定 |
| 対応端末 | メモリ上限・SharedArrayBuffer の可否で落ちる端末がある | 端末を選ばない |
| 費用 | ゼロ | 1本あたりの実測値（T5-4） |
| 実装量 | COOP/COEP ヘッダの付与が要る（next.config.ts を触らずに済むか要確認） | ワーカーの構築・認証・鍵管理 |
| 審査での説明 | 「動画はサーバーへ送られない」と言い切れる | 「処理中のみ保持し即時削除」と説明が要る |

**COOP/COEP の点は特に注意。** ffmpeg.wasm は SharedArrayBuffer を要求することがあり、
そのためのヘッダを付けると **同一オリジンの他のページ（/portal/ や /event/）に
影響しうる**。next.config.ts を触らない原則（repository-structure.md §5-2）と
衝突しないか、ヘッダの適用範囲をパス単位に限定できるかを先に確かめること。
ここが解けないなら ffmpeg.wasm は不採用でよい。`,
  },
];
