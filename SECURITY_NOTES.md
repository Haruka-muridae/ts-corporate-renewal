# セキュリティ上の注意と既知の制約

TSAM AI の本番認証システム（`gas-auth/` と `/login/` ほか）について、
**何が守れていて、何が守れていないか**を明示する。

「安全です」ではなく「この条件で、ここまで守れる」という形で書く。

---

## 1. 静的ホスティングの限界（最重要）

このサイトの**大部分は Vercel 上の静的ファイル**として配信される（`public/` 配下）。
静的ファイルについては、**サーバー側でリクエストを止める仕組みが無い。**

例外は `app/event/` 配下（交流会申込アプリ）で、こちらは Next.js の
サーバー実行がある。以下の「限界」は静的ファイル側の話であり、
`app/event/` の API ルートには当てはまらない。

### 守れるもの

| 対象 | 守り方 |
| --- | --- |
| 利用者データ（メール・契約状態・ロール） | Apps Script API がセッショントークンを検証してから返す |
| パスワード | ハッシュのみ保存。API は照合だけを行い、ハッシュを返さない |
| 決済操作 | Checkout Session の作成はサーバー側のみ。Price ID もサーバーが持つ |
| 権限（管理者判定） | `users` シートの `role` 列をサーバーが読む |

### 守れないもの

| 対象 | 理由 |
| --- | --- |
| HTML・JS ファイルそのもの | URLを知っていれば誰でも取得できる |
| 画面の構造・文言 | 上と同じ |
| クライアント側のコードの改変 | 利用者のブラウザ上のことなので防げない |

つまり **`/portal/` の HTML と JS は、未ログインでも取得できる。**
できないのは「中身を表示すること」と「データを得ること」である。

`portal/index.html` は保護対象を `hidden` にした状態で配信され、
`portal.js` はサーバーがセッションを有効と答えるまで `hidden` を外さない。
`localStorage` を偽造しても、サーバーに存在しないトークンは検証で落ちる
（自動テストで確認済み）。

### したがって

**秘密にすべき情報を HTML や JS へ直接書かないこと。**
本番アプリを追加するときも、機密データは必ず Apps Script API 経由で取得する。

新しい保護対象ページを作る場合は、必ず次の形にする。

```js
import { setScreenDepth } from '../../auth/config.js';
import { guardPage } from '../../auth/session.js';

setScreenDepth(2);                 // 階層に合わせる

const user = await guardPage();
if (!user) return;                 // ここで描画を止める（すでにログイン画面へ遷移済み）

render(user);                      // 確認が済んでから描画する
```

---

## 2. パスワードハッシュ

### 採用方式

```text
PBKDF2-HMAC-SHA256
  反復回数: 既定 10,000（設定シート PBKDF2_ITERATIONS で変更可、下限1,000）
  出力長:   32バイト
  ソルト:   利用者ごとに16バイト（毎回ランダム生成）
  pepper:   保存前に HMAC-SHA256(結果, PASSWORD_PEPPER)
```

保存形式は `pbkdf2$sha256$<反復回数>$<16進64文字>`。
反復回数を値の中に持つため、後から回数を増やしても既存利用者は締め出されない
（次回ログイン成功時に静かに作り直される）。

実装は `Crypto.gs` の `pbkdf2Sha256Hex_`。
RFC 8018 どおりの値になることを、Node の `crypto.pbkdf2Sync` と
1バイト単位で突き合わせて確認している（日本語・絵文字を含むパスワードも含む）。

### 反復回数が少ないことの正直な説明

**10,000 回は現代の推奨値より少ない。**
OWASP は PBKDF2-SHA256 で 600,000 回程度を推奨している。

理由は Apps Script の実行速度である。
Apps Script には PBKDF2 が無く、`Utilities.computeHmacSha256Signature` を
自前で反復するしかない。1回ごとに Java 側の呼び出しが発生するため、
回数を増やすとログイン1回に何十秒もかかり、実用にならない。

### どう補っているか

**pepper（`PASSWORD_PEPPER`）を Script Properties に置いている。**

- スプレッドシートには pepper が存在しない。
- したがって「スプレッドシートだけが漏れた」場合、
  攻撃者は pepper を知らないため、総当たりを始めることすらできない。
- pepper が変わると照合できなくなることを自動テストで確認している。

pepper と users シートの両方が漏れた場合は、
10,000 回相当の強度しかない。その前提で運用すること。

### 運用者がすべきこと

1. `benchmarkPasswordHashing()` で実測し、
   **ログイン1回が 0.5〜1.5 秒に収まる最大の値**を設定する。
2. `PASSWORD_PEPPER` を Script Properties から漏らさない。
   バックアップは取る（**失うと全利用者がパスワード再設定を強いられる**）。
3. スプレッドシートと Drive フォルダを一般公開しない。
   共有設定は「制限付き」のままにする。

### 禁止事項（実装で守っている）

- 平文パスワードの保存 … しない
- パスワードのログ出力 … しない（自動テストで確認）
- パスワードのメール記載 … しない（一時URLのみ）
- ハッシュのフロント返却 … しない（`toPublicUser_` が除外）
- 管理画面でのパスワード表示 … 表示する画面自体が無い

---

## 3. Stripe Webhook の署名検証

### 制約

**Apps Script の `doPost(e)` には HTTP リクエストヘッダーが渡らない。**
Stripe の `Stripe-Signature` ヘッダーを Apps Script 単体では受け取れず、
標準的な署名検証をそのままの形では成立させられない。

**この制約を隠して「署名検証済み」と報告することはしない。**

### 採用している検証

| # | 方法 | 必須 | 内容 |
| --- | --- | --- | --- |
| 1 | URLの合言葉 | ○ | `?k=` を `STRIPE_WEBHOOK_URL_KEY` と定数時間比較 |
| 2 | **Stripe API への照会** | ○ | 本文からは `event.id` だけを採り、`GET /v1/events/{id}` の結果だけを処理に使う |
| 3 | HMAC-SHA256 署名検証 | 中継を置く場合のみ | `verifyStripeSignature_()` で検証 |

**#2 が本質的な検証である。**

- 受信した本文は「イベントIDの入れ物」としてしか使わない。
- 処理に使うのは、こちらの秘密鍵で Stripe から取得し直した内容だけ。
- 攻撃者は自分では Stripe アカウント内にイベントを作れないため、
  偽の本文を送っても照会が失敗して処理が止まる。
- 本文を差し替えても照会結果だけが使われることを自動テストで確認している
  （偽メールアドレスでの利用者作成が起きないこと）。

#3 は中継（Cloudflare Workers など）を置いた場合のみ有効になる。
構成は [STRIPE_SETUP.md](./STRIPE_SETUP.md) を参照。

### 冪等性

`stripe_events` シートに `event_id` を記録し、
受信済みのイベントは二度処理しない。
記録と重複判定は同じ `LockService` のロックの中で行うため、
同時に2回届いても片方だけが処理へ進む。

Stripe の再送（構成Aで 302 により失敗表示になった場合など）が起きても、
利用者の重複作成やメールの再送は発生しない。

### 合言葉が漏れた場合

`STRIPE_WEBHOOK_URL_KEY` が漏れても、#2 の照会があるため
偽イベントを処理させることはできない。
ただし総当たり的な POST を受け続けることになるため、
気づいた時点で新しい値へ変更し、Stripe 側のURLも更新すること。

---

## 4. セッション

```text
トークン: 32バイト相当（base64url 43文字）のランダム値
保存:     サーバー側に HMAC-SHA256(トークン, SESSION_SECRET) のみ
ブラウザ: localStorage にトークン文字列だけ
期限:     通常 12時間 / ログイン保持 30日（設定で変更可）
```

### 設計の要点

- **ブラウザ側に判断材料を持たせない。**
  トークンからは利用者IDもロールも期限も読めない。
- `localStorage` を書き換えても、サーバーに無いトークンは検証で落ちる。
- 検証のたびにアカウント状態と契約状態を再確認する。
  契約が切れた・アカウントを止めた場合、次のアクセスで締め出される。
- 有効期限は検証時に延長しない（延長すると実質無期限になる）。
- パスワード変更時は、その利用者のセッションを **すべて** 失効させる。

### 乱数について

Apps Script には `crypto.getRandomValues` が無い。
`Utilities.getUuid()`（RFC 4122 v4、1つあたり122ビット）を3つ連結し、
現在時刻とともに SHA-256 で圧縮している。

`getUuid()` の実装品質に依存する点は残るが、
Google の提供する UUID v4 が予測可能である前提は置いていない。
より強い保証が必要なら、乱数生成を外部へ移す構成が必要になる。

### localStorage を使う理由と影響

`sessionStorage` ではタブを閉じると消えるため、
「ログイン状態を保持する」を実現できない。

`localStorage` は XSS があれば読み取られる。
そのため、このシステムでは:

- 文字列の挿入はすべて `textContent`。`innerHTML` を使っていない。
- 外部スクリプトは Google Fonts のCSSのみ（JSは読み込まない）。
- 利用者入力を DOM へ流す経路を作っていない。

CSP（Content-Security-Policy）は **未導入**。

Pages 時代はヘッダーを制御できないことが理由だった。
**Vercel 移行（2026-08-01）でその制約は無くなっている**
（`next.config.ts` の `headers()` などで指定できる）。
導入していないのは、まだ着手していないためであって、できないからではない。
`<meta http-equiv>` での指定も入れていない。

---

## 5. 一時トークン（パスワード初期設定・再設定）

```text
トークン: 32バイト相当（base64url 43文字）
保存:     HMAC-SHA256(トークン, TOKEN_SECRET) のみ
期限:     初期設定 72時間 / 再設定 60分（設定で変更可）
使用:     1回限り。使用時に used_at を埋める
```

- 単純な SHA-256 ではなく鍵付きにしている。
  シートが漏れても、`TOKEN_SECRET` なしには総当たりできない。
- 検証と使用済み化を1つのロックで囲むため、同じトークンで2回設定できない。
- 新しく発行するとき、同じ利用者の未使用トークンをすべて無効化する。
- 画面側は読み取り後すぐ URL からトークンを消す（`history.replaceState`）。
  履歴・共有・referrer 経由での流出を減らすため。
- 該当ページは `<meta name="referrer" content="no-referrer">` を指定している。

---

## 6. アカウント列挙の防止

「そのメールアドレスが登録されているか」を外部から判別できないようにしている。

| 場面 | 対応 |
| --- | --- |
| ログイン失敗 | 未登録・不一致・契約切れ・停止中をすべて同じ文言で返す |
| ログイン失敗（時間差） | 未登録の場合もダミーのハッシュ計算を行い、応答時間を揃える |
| パスワード再設定の申し込み | 登録の有無にかかわらず同じ応答・同じ文言 |
| 停止中アカウントへの再設定申し込み | メールを送らないが、応答は同じ |
| ログアウト | 存在しないトークンでも成功として返す |

本当の失敗理由は `login_logs` の `failure_reason_code` にのみ残す。

**残る差分:** 未登録アドレスにはメールが届かない。
これは仕組み上避けられない（送るわけにいかない）。

---

## 7. ログイン失敗制限

```text
既定: 5回連続失敗 → 15分間ロック
```

- ロック中は照合そのものを行わない（総当たりに計算資源を与えない）。
- ログイン成功で失敗回数とロックを解除する。
- 存在しないメールアドレスに対しては行を作らない
  （他人のアドレスを指定して締め出す攻撃を防ぐ）。

**残る制約:** Apps Script は送信元IPを取得できないため、
IP単位のレート制限はかけられない。
アドレスを変えながらの総当たりは、この仕組みでは止まらない。
1アカウントあたりの試行回数は確実に制限される。

---

## 8. ログに残さないもの

`login_logs` / `admin_action_logs` / `system_error_logs` に
以下を書かない（自動テストで確認）。

- パスワード（平文・ハッシュとも）
- ソルト
- セッショントークン・一時トークン
- Stripe のシークレットキー・Webhook シークレット・URL合言葉

メールアドレスは `t***@example.com` の形にマスクして記録する。
User-Agent は `windows/chrome` のような要約だけを残す。

---

## 9. 排他制御

`LockService` のスクリプトロックで直列化している箇所:

- 利用者の作成（重複行の防止）
- ログイン失敗回数の更新（数え漏れの防止）
- トークンの検証と使用済み化（二重使用の防止）
- セッションの発行
- Webhook の受信記録（二重処理の防止）

ロックは入れ子で呼んでも安全にしてある（`Store.gs` の `withLock_`）。
Apps Script のロックはスクリプト単位のため、
素直に二重取得すると自分自身を待ってタイムアウトする。
深さを数えて最外周だけが実際のロックを取る。

ロックを取れなかった場合は例外を投げ、
利用者には「アクセスが集中しています」と返す。**黙って処理を続けない。**

---

## 10. 秘密情報の置き場所

| 情報 | 置き場所 | リポジトリ | スプレッドシート |
| --- | --- | --- | --- |
| Stripe シークレットキー | Script Properties | × | × |
| Stripe Webhook シークレット | Script Properties | × | × |
| Webhook URL の合言葉 | Script Properties | × | × |
| セッション用シークレット | Script Properties | × | × |
| トークン用シークレット | Script Properties | × | × |
| パスワード pepper | Script Properties | × | × |
| Stripe Price ID | `plans` シート | × | ○ |
| Apps Script Web アプリURL | `public/auth/config.js` | ○ | ― |

`Config.gs` の `SECRET_KEYS` により、
秘密情報キーは設定シート経由では読み出せないようにしている
（誤って設定シートに書いても、コードは参照しない）。

`public/auth/config.js` の `apiUrl` は公開エンドポイントであり秘密ではない。
このリポジトリには実URLを入れていない（空のままコミットしている）。

### Drive とスプレッドシートの共有設定

**一般公開しないこと。** 共有設定は「制限付き」のままにする。

`users` シートにはメールアドレスとパスワードハッシュが入る。
公開すると、pepper が無くても総当たりの土俵に乗せてしまう。

---

## 11. HTTPS

Vercel がドメインへ証明書を自動発行し、HTTP は HTTPS へ転送する。
リポジトリ側で有効化する設定は無い（`CNAME` は削除済み）。

Apps Script の Web アプリは常に HTTPS。
`public/auth/config.js` の `isApiConfigured()` は `https://script.google.com/...` 以外を
設定済みと判定しないため、誤って HTTP のURLを入れても通信しない。

---

## 12. まだ実装していないこと

正直に列挙する。

| 項目 | 状況 |
| --- | --- |
| 二段階認証（TOTP） | 未実装。`/apps/` 側には Supabase 前提の実装があるが、本番系には入れていない |
| CSP（Content-Security-Policy） | 未導入。Vercel 移行でヘッダーは制御できるようになったが、まだ着手していない（§9） |
| IP単位のレート制限 | 不可能（Apps Script が送信元IPを取得できない） |
| セッションの端末一覧・個別失効UI | 未実装。`sessions` シートを直接編集すれば失効できる |
| 管理画面 | 未実装。利用者の停止・再開はスプレッドシートを直接編集する |
| メール送信の到達確認 | 未実装。MailApp の送信結果しか分からない |
| パスワードの使い回し検出 | 未実装 |
| 監査ログの改ざん防止 | 未実装（スプレッドシートを編集できる人は書き換えられる） |

---

## 13. 運用時に確認すること

### 定期的に

- `system_error_logs` シートを確認する（Webhook の失敗、メール送信の失敗）
- `login_logs` の失敗が特定のアカウントに集中していないか確認する
- `stripe_events` に `failed` の行が無いか確認する

### 変更したとき

- Script Properties を変更したら `checkAuthSetup()` を実行する
- `PBKDF2_ITERATIONS` を変えたら `benchmarkPasswordHashing()` で所要時間を確認する
- Apps Script を再デプロイしたら、`/exec` URL が変わっていないか確認する
  （「デプロイを管理」→ 既存デプロイの編集なら URL は変わらない。
  「新しいデプロイ」を作ると別URLになり、`public/auth/config.js` と Stripe の
  Webhook URL の更新が必要になる）

### 事故が起きたら

| 事象 | 対応 |
| --- | --- |
| `PASSWORD_PEPPER` が漏れた | 変更する。全利用者にパスワード再設定を依頼する |
| `SESSION_SECRET` が漏れた | 変更する。全セッションが即座に無効になる |
| `TOKEN_SECRET` が漏れた | 変更する。発行済みの一時リンクが無効になる |
| `STRIPE_SECRET_KEY` が漏れた | Stripe ダッシュボードでキーをローテートし、Script Properties を更新 |
| Webhook 合言葉が漏れた | 新しい値に変え、Stripe 側のURLを更新 |
| スプレッドシートが漏れた | pepper を変更し、全利用者にパスワード再設定を依頼する |
| 特定利用者を止めたい | `users` シートの `account_status` を `disabled` にし、`sessions` の該当行に `revoked_at` を入れる |
