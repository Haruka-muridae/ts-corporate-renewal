# Set the approved canonical origin without a path (for example, https://example.com).
# TODO: Replace the empty value after the production domain is approved.
NEXT_PUBLIC_SITE_URL=

# Use "production" only for the public production deployment.
# Local builds and Cloudflare preview deployments must use "preview".
NEXT_PUBLIC_DEPLOY_ENV=preview
