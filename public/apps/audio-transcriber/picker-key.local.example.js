/*
 * Google Picker のブラウザ用APIキーの雛形。
 *
 * 使い方:
 *   1. このファイルを同じディレクトリへ picker-key.local.js という名前でコピーする
 *   2. PICKER_API_KEY に、Google Cloud Console で作ったブラウザ用キーを入れる
 *   3. ページを再読み込みする
 *
 * picker-key.local.js は .gitignore に入れてあり、Git の追跡対象外である。
 * **このファイル（.example）には実キーを書かないこと。** 雛形なので追跡される。
 *
 * 名刺スキャナ（apps/card-scanner/）と同じ方式にそろえてある。
 *
 * ------------------------------------------------------------------
 * キーに必ず制限を掛けること
 * ------------------------------------------------------------------
 * 静的サイトである以上、APIキーは配信されるJSに含まれ、閲覧者から見える。
 * ファイルを分けるのは「リポジトリに残さない」ためであって、
 * キーを秘密にできるわけではない。実質的な防御は次の2つだけである。
 *
 *   ・アプリケーションの制限 … ウェブサイト（HTTPリファラー）
 *       https://tsam-ai.com/*
 *       http://localhost:8000/*
 *   ・APIの制限 … Google Picker API のみ
 *
 * サーバー用キー・Gemini APIキー・OAuth client secret は絶対に入れない。
 * ------------------------------------------------------------------
 *
 * このファイルが無くても音声文字起こしアプリは動作する。
 * その場合 Picker だけが無効になり、Drive からの選択は
 * 「当社アプリが保存した音声の一覧」に切り替わる。
 */

export const PICKER_API_KEY = '';
